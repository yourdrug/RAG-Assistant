# Vite API URL — used at build time (baked into the SPA) and at runtime.
# In Docker the client nginx proxies /api/* to server:8001, so use /api.
# For local dev without Docker, point to the backend directly.
VITE_API_URL=/api
