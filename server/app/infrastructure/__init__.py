"""Infrastructure layer -- concrete implementations of domain ports and application services.

Contains database access (SQLAlchemy), vector store (Qdrant), ML model clients
(Ollama, HuggingFace), file storage (local / S3), event bus, authentication,
scheduling, and all repository implementations.
"""
