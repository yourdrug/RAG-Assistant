"""Domain layer -- business logic, entities, value objects, repository interfaces, and domain services.

This layer is framework-agnostic: no infrastructure imports, no ORM, no HTTP.
All business rules live here; concrete implementations live in the
infrastructure layer.
"""
